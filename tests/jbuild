(jbuild_version 1)

(executable
 ((name test_distributed)
  (modes (native))
  (libraries (distributed lwt lwt.unix logs.lwt alcotest))))

(alias
 ((name    runtest)
  (deps    (test_distributed.exe))
  (package distributed-lwt)
  (action  (run ${<}))))